# Dependencies
node_modules/

# Environment variables – NEVER commit secrets
.env

# Runtime state file
data/

# Dusk's local toolkit (not part of the bot)
dusk-check.js
exports/

# Design docs / writing-plans (Traine: design lives outside the
# Railway deploy artifact - keep this folder untracked so future plan
# documents do not ship to production).
docs/

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
